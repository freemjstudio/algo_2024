# https://leetcode.com/problems/spiral-matrix/

class Solution:
    def spiralOrder(self, matrix: List[List[int]]) -> List[int]:
        result = []
        
        # 우, 하, 좌, 상 
        dx = [1, 0, -1, 0]
        dy = [0, 1, 0, -1]
        
        n = len(matrix)
        m = len(matrix[0])
        
        while 
        
        return result 
        